# Core dependencies
PyYAML>=6.0.2
tqdm>=4.66
imageio-ffmpeg>=0.5.1
faster-whisper>=1.2.1
numpy>=1.26
soundfile>=0.12
openai>=1.0.0
httpx>=0.23.0

# Search tools for lyrics lookup
ddgs>=9.0.0

# GPU support for faster-whisper / CTranslate2 (CUDA 12 + cuDNN 9)
# pip install -r requirements-cu12.txt
